name: Print Hello world
on:
  push:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello 
        run: echo "Hello world"

      - name: setup
        uses: actions/checkout@v4
        
      - name: test function
        run: node start.js
