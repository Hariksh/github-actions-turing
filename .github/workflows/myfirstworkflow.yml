name: Print Hello world
on:
  push:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello 
        run: echo "Hello world"

  add:
    runs-on: ubuntu-latest
    steps: 
    - name: test function
      run: node start.js
      